{{ form_start(userForm) }}

<div class="mb-4 text-pink-500 bold">
    {{ form_row(userForm.roles, {'id': 'roles', 'class': 'shadow appearance-none border text-pink-500 rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline'}) }}
</div>


<button type="submit" class="text-white bg-gradient-to-br from-pink-500 to-orange-400 hover:bg-gradient-to-bl focus:ring-4 focus:outline-none focus:ring-pink-200 dark:focus:ring-pink-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center me-2 mb-2">
 {{ button_label|default('Modifier role') }}
</button>

{{ form_end(userForm) }}
