{{ form_start(form) }}
    <div class="flex justify-between items-center mb-4">
        {{ form_widget(form) }}
        <button type="submit" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}
